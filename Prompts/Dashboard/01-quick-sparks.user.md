QUICK SPARK REQUEST

Requested count:
{{count}}

Optional direction:
{{direction}}

Recent session sparks to avoid repeating, if any:
{{recent_sparks_json}}

Create exactly the requested number of one-sentence sparks. Keep them compact enough to scan on a dashboard card. Return only valid JSON matching the quick-sparks schema.